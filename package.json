{
  "name": "MCP Server and Client",
  "main": "src/server.ts",
  "scripts": {
    "server:build": "tsc",
    "server:dev": "tsx src/server.ts",
    "server:build:watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server:inspect": "set DANGEROUSL_OMIT_AUTH=true && npx @modelcontextprotocol/inspector npm run server:dev"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.16.1",
    "@types/node": "^20.11.17",
    "tsc": "^2.0.4",
    "tsx": "^3.12.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0",
    "zod": "^3.25.76"
  }
}
